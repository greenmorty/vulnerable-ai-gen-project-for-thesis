/**
 * Responsibility: Configures the Vite development server and React plugin for the frontend workspace.
 */
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
  },
});

